// Unser Programm ist modular, dh es verwendet das modulare JDK
// => wir müssen alle Module, die wir verwenden wollen, registrieren
module swingDemos {
	// Das Modul in dem Swing (und AWT) enthalten ist.
	requires java.desktop;
}